import { Fragment } from "react";

const HotelCardDescription = (props) => {
    return (
        <Fragment>
            {props.text}
        </Fragment>
    )
};

export default HotelCardDescription;